import React from 'react';
import { withRouter, Link } from 'react-router-dom';

const NavBar = () => {
    return (
        <header>
            <h1>BetaData</h1>
        </header>
    )
};

export default withRouter(NavBar);